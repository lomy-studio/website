# Static site: no build step, publish the repo root as-is.
[build]
  publish = "."
  command = ""

# Clean URL for the contact page.
[[redirects]]
  from = "/contact"
  to = "/contact.html"
  status = 200
